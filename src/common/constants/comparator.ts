export enum comparator {
   'does not equal' = '!=',
   'equals' = '=',
   'is after' = '>',
   'is before' = '<',
   'is greater than or equal to' = '>=',
   'is greater than' = '>',
   'is less than or equal to' = '<=',
   'is less than' = '<',
}