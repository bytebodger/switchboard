import type { GenericFunction } from '../../../../../common/types/GenericFunction';

interface Props {
   campaignId: number,
   onClose: GenericFunction,
   open: boolean,
}

export const CampaignTagDialog = ({ campaignId, onClose, open }: Props) => {
   return <></>
}